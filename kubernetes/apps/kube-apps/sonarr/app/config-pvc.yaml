---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config
  namespace: kube-apps
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: openebs
  resources:
    requests:
      storage: 256Mi
