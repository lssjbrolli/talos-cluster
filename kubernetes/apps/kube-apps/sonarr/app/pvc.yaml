---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config
spec:
  accessModes:
    - ReadWriteOnce
  # dataSourceRef:
  #   kind: ReplicationDestikube
  #   name: sonarr-restorenation
  #   apiGroup: volsync.bac
  resources:
    requests:
      storage: 5Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-data
spec:
  storageClassName: zfs-storage
  accessModes:
    - ReadWriteOnce
  volumeName: sonarr-pv
  resources:
    requests:
      storage: 21Ti
