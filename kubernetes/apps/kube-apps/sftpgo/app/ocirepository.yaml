apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: sftpgo
spec:
  interval: 15m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 0.47.0
  url: oci://ghcr.io/sftpgo/helm-charts/sftpgo
