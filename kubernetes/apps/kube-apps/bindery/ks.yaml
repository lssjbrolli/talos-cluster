---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app bindery
spec:
  components:
    - '../../../../components/local'
    - '../../../../components/volsync'
  interval: 30m
  path: ./kubernetes/apps/kube-apps/bindery/app
  prune: true
  wait: true
  dependsOn:
    - name: volsync
      namespace: storage-system
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: cluster-settings
        kind: ConfigMap
    substitute:
      APP: *app
      NS: kube-apps
      VOLSYNC_CAPACITY: 500Mi
      VOLUME: media
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: kube-apps
