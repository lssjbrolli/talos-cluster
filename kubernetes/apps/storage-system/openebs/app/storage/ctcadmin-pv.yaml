apiVersion: v1
kind: PersistentVolume
metadata:
  name: ctcadmin-pv # some unique name
spec:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 10Gi # size of the volume
  claimRef:
    apiVersion: v1
    kind: PersistentVolumeClaim
    name: ctcadmin-data # name of pvc which can claim this PV
    namespace: storage-system # namespace for the pvc
  csi:
    driver: zfs.csi.openebs.io
    fsType: zfs
    volumeAttributes:
      openebs.io/poolname: storage/backup # change the pool name accordingly
    volumeHandle: ctcadmin # This should be same as the zfs volume name
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - talos-cluster # change the hostname where volume is present
  persistentVolumeReclaimPolicy: Retain
  storageClassName: zfs-storage
  volumeMode: Filesystem
