apiVersion: v1
kind: PersistentVolume
metadata:
  name: minio-pv # some unique name
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 150Gi # size of the volume
  claimRef:
    apiVersion: v1
    kind: PersistentVolumeClaim
    name: minio-data # name of pvc which can claim this PV
    namespace: storage-system # namespace for the pvc
  csi:
    driver: zfs.csi.openebs.io
    fsType: zfs
    volumeAttributes:
      openebs.io/poolname: storage/backup # change the pool name accordingly
    volumeHandle: minio # This should be same as the zfs volume name
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - talos-cluster # change the hostname where volume is present
  persistentVolumeReclaimPolicy: Delete
  storageClassName: zfs-storage
  volumeMode: Filesystem
