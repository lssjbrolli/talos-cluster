apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cluster.yaml
  - ./ocirepository.yaml
  - ./service.yaml
configMapGenerator:
  - name: immich-db
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
