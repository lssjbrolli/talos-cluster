apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cluster.yaml
  - ./ocirepository.yaml
  - ./prometheusrule.yaml
  - ./service.yaml
configMapGenerator:
  - name: cluster17
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
