kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: prowlarr-config
spec:
  storageClassName: democratic-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
