---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${APP}-data
  namespace: ${NS}
spec:
  storageClassName: talos
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 24Ti
  
