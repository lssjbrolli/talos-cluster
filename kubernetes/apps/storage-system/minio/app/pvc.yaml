kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: minio-data
spec:
  storageClassName: zfs-storage
  volume-name: minio-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 150Gi
