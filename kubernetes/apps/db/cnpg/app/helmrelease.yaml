apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg
  namespace: db
spec:
  releaseName: cnpg
  chartRef:
    kind: OCIRepository
    name: cnpg
  interval: 30m
  valuesFrom:
    - kind: ConfigMap
      name: cnpg
