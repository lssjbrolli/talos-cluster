kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ctcadmin-data
spec:
  storageClassName: zfs-storage
  volumeName: ctcadmin-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
