apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./pg.sops.yaml
configMapGenerator:
  - name: cnpg
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
