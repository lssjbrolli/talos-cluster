---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: rd-tcp
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: rd-tcp
  rules:
    - backendRefs:
        - name: rustdesk
          namespace: kube-apps
          port: 21116
