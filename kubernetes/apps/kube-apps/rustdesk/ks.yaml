apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app rustdesk
  namespace: flux-system
spec:
  components:
    - "../../../../components/kopiur"
  interval: 30m
  path: ./kubernetes/apps/kube-apps/rustdesk/app
  prune: true
  wait: true
  dependsOn:
    - name: kopiur-repository
      namespace: storage-system
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
      - name: cluster-config
        kind: ConfigMap
    substitute:
      APP: *app
      NS: kube-apps
      KOPIUR_SCHED: "H */3 * * *"
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: kube-apps
